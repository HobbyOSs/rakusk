# Active Context

## 現在の焦点
- **BDO（ジャンプ最適化）の完成とメインブランチへの統合**
- 全テストケース（Day 25 まで）のパス確認。

## 直近のタスク
- [x] `harib12c`, `harib19b` 等で発生していた「2バイトの不一致」の原因特定と修正。
- [x] `t/day22_harib19c.t` および `t/day25_harib22f.t` のスキップ解除と正常終了の確認。
- [x] 全テストスイートの回帰テスト実行 (`make test`)。
- [x] `main` ブランチへのマージとプッシュ。

## 最近の変更点
- **ジャンプ最適化（BDO）の完成**: 
    - 相対ジャンプの楽観的初期化の制限、単調増加の保証、セクションPCリセットの導入により、複雑な OS 構成コード（haribote OS の naskfunc.nas 相当）の完全なアセンブルを実現。
- **テストスキップの全解除**: 
    - 文法やロジックの不一致によりスキップされていた `harib19c`, `harib22f` のテストをすべて有効化し、バイナリ一致を確認。

## 課題と次のステップ
- [ ] パフォーマンスの最適化（Grammar のパース速度向上など）。
- [ ] エラーメッセージの改善。
- [ ] さらなる OS サンプルコードの追加と検証。

## 得られた知識
- **BDO（マルチパス最適化）における「楽観的初期化」の罠**: 
    - 全ての JMP を 2バイトと仮定すると、短縮不可能な間接ジャンプ（最低4バイト以上）が破壊的な影響を受ける。初期化対象は、オペランドが `Immediate`（ラベル等）である相対ジャンプに限定する必要がある。
- **COFF セクションのアドレス計算**: 
    - COFF形式では各セクションがアドレス 0 から始まるため、アセンブラ内部の PC もセクションごとにリセットしなければ、アライメント計算がズレる。
- **Pass 1 と Pass 2 の不一致によるシンボルズレ**: 
    - 命令サイズが Pass 1 (サイズ見積もり) と Pass 2 (エンコード) で異なると、バイナリデータ自体が一致していてもシンボルテーブル内のアドレスがずれる原因となる。